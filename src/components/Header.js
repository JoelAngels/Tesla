import React from "react";

const Header = () => {
  return (
    <div>
      <h1>Lets build the header</h1>
      <h1>Lets build the header</h1>
    </div>
  );
};

export default Header;
